use handsonassignment;
select distinct(course_code) from course_fees;
select distinct(Base_Fees) from course_fees;
select distinct(Special_Fees) from course_fees;

select distinct(Course_Code) from course_fees_history;
select distinct(Base_Fees) from course_fees_history;
select distinct(Special_Fees) from course_fees_history;




